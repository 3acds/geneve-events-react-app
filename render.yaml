services:
  - type: web
    name: geneva-events-web
    runtime: static
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        value: https://geneva-events-api.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
